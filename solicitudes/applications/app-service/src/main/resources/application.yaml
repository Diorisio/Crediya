server:
  port: 8081
spring:
  application:
    name: "ReactiveProject"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    include: null
management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
  health:
    circuitbreakers:
      enabled: true
cors:
  allowed-origins: "http://localhost:4200,http://localhost:8081"
adapters:
  r2dbc:
    host: "localhost"
    port: 3306
    database: "prestamos"
    username: "root"
    password: "Albert1879*"
logging:
  file:
    name: "logs/miapp.log"
  level:
    root: "INFO"
    org.springframework: "INFO"
    co.com.pragma: "DEBUG"
adapter:
  restconsumer:
    timeout: 5000
    url: "http://localhost:8080"
  sqs:
    region: "us-east-2"
    queueUrl: "http://localhost:4569/000000000000/sample"
    resilience4j: null
  circuitbreaker:
    instances:
      testGet:
        registerHealthIndicator: true
        failureRateThreshold: 50
        slowCallRateThreshold: 50
        slowCallDurationThreshold: "2s"
        permittedNumberOfCallsInHalfOpenState: 3
        slidingWindowSize: 10
        minimumNumberOfCalls: 10
        waitDurationInOpenState: "10s"
      testPost:
        registerHealthIndicator: true
        failureRateThreshold: 50
        slowCallRateThreshold: 50
        slowCallDurationThreshold: "2s"
        permittedNumberOfCallsInHalfOpenState: 3
        slidingWindowSize: 10
        minimumNumberOfCalls: 10
        waitDurationInOpenState: "10s"
jwt:
  secret-key: "aVeryStrongSecretKeyThatIsAtLeast32CharsLong!!"
  expiration: 3600
entrypoint:
  sqs:
    region: "us-east-1"
    endpoint: "http://localhost:4567"
    queueUrl: "http://localhost:4567/000000000000/sample"
    waitTimeSeconds: 20
    maxNumberOfMessages: 10
    visibilityTimeoutSeconds: 10
    numberOfThreads: 1
